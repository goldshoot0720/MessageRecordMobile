MessageRecord 1.0.5 — Meowa 可愛彩色圖示。

- 全套 UI 圖示改為 Meowa 產出的彩色可愛 PNG（各密度 drawable），不再使用自繪向量路徑。
- Launcher 換成笑臉鈴鐺＋盾牌勾勾的軟 3D 造型；adaptive icon 前景為透明吉祥物、深藍底。
- 圖示顏色固定、不跟隨主題 tint。

安裝：Android 8.0 以上，套件 ID `com.notiguard.debug`，versionCode 6，可直接覆蓋 v1.0.4 預覽版。此版本仍為 Debug 簽署預覽 APK。

驗證：單元測試、assembleDebug、lintDebug（0 errors）。
